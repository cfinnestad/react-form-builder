import { FieldItem } from "../../Items";
import { Options } from "../../../Builder/Builder";
declare const NumberValidate: (item: FieldItem, options: Options) => boolean;
export default NumberValidate;
