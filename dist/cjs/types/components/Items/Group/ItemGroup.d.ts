import React from "react";
import { GroupProps } from "../Items";
declare const ItemGroup: ({ item, items, options, activeItem, setActiveItem, errorHandler }: GroupProps) => React.JSX.Element;
export default ItemGroup;
