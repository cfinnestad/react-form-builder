import { Options } from "../../Actions/Actions"

export interface BuildProps {
    Items: Item[],
    ItemTypes: object,
    Options: Options
}
const BuildItems = ({Items, ItemTypes, Options}: BuildProps) => {
    return <></>
}

export default BuildItems